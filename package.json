{
  "name": "whitecadae",
  "version": "1.0.0",
  "private": true,
  "description": "WhiteCadae — plateforme d'explications des textes de White Cadae (Cloudflare Workers + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute whitecadae-db --remote --file=./schema.sql",
    "db:seed": "wrangler d1 execute whitecadae-db --remote --file=./seed.sql",
    "db:local": "wrangler d1 execute whitecadae-db --local --file=./schema.sql && wrangler d1 execute whitecadae-db --local --file=./seed.sql"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}
